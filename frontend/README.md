# Student Analytics Frontend

Vite + React frontend for the Student Analytics Dashboard.

Setup

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:5000` with analytics endpoints.
